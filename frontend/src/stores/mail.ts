/**
 * @file mail.ts
 * @description Simplified entrypoint access for the mail store.
 * Re-exports the unified main `useMailStore` from the `mail/index.ts` submodule.
 */

export { useMailStore } from './mail/index.ts'
